kind: pipeline
name: default

steps:
  - name: test
    image: python
    commands:
    - cd tests
    - pwd
    - pip install -r requirements.txt

  - name: test2
    image: ubuntu
    commands:
    - ls