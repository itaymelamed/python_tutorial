kind: pipeline
name: default

steps:
  - name: test
    image: python
    commands:
    - cd tests
    - pwd
    - pip install -r requirements.txt

  - name: web
    image: python
    commands:
    - cd web
    - python app.py