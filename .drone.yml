kind: pipeline
name: default

steps:
  - name: app
    image: python
    commans:
    - cd web
    - pwd

  - name: hello
    image: ubuntu
    commans:
    - pwd

  - name: test
    image: python
    commands:
    - cd tests
    - pwd
    - pip install -r requirements.txt
    - pytest -vvv